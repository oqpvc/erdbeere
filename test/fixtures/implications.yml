# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

comm_and_lnoeth_implies_rnoeth:
  atoms: ring_is_comm, ring_is_lnoeth
  implies: ring_is_rnoeth

comm_and_rnoeth_implies_lnoeth:
  atoms: ring_is_comm, ring_is_rnoeth
  implies: ring_is_lnoeth

fg_and_noeth_base_ring_implies_noeth:
  atoms: module_is_fg, base_ring_is_lnoeth
  implies: module_is_noeth
  
